RAG_VECTOR_BACKEND=pgvector
DATABASE_URL=postgresql://daeum:daeum@127.0.0.1:5433/daeum_hangeoreum

# Required for ingestion/search embeddings and AI generation.
# Keep the real value in .env.local or .env.pgvector.local; never commit it.
OPENAI_API_KEY=
